<adonisControls:AdonisWindow x:Class="clawSoft.clawPDF.Shared.Views.SignaturePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
        xmlns:converter="clr-namespace:clawSoft.clawPDF.Shared.Converter"
        xmlns:viewModels="clr-namespace:clawSoft.clawPDF.Shared.ViewModels"
        Title="clawPDF"
        Height="205"
        Width="369"
        WindowStartupLocation="CenterScreen"
        Loaded="OnLoaded">
    <Window.DataContext>
        <viewModels:SignaturePasswordViewModel />
    </Window.DataContext>
    <Window.Resources>
        <converter:BoolToVisibilityConverter x:Key="VisibleWhenTrue" TrueValue="Visible" FalseValue="Collapsed" />
        <converter:BoolToVisibilityConverter x:Key="CollapsedWhenTrue" TrueValue="Collapsed" FalseValue="Visible" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="3" Grid.Column="0" Margin="10">
            <Label Name="PasswordLabel" Target="{Binding ElementName=PasswordBox}">_Password:</Label>
            <DockPanel>
                <Viewbox DockPanel.Dock="Right" Stretch="Uniform" Width="18"
                         Visibility="{Binding StorePasswordCommand.IsExecutable, Converter={StaticResource VisibleWhenTrue}}">
                    <Path Data="{StaticResource Accept}" Fill="#6fae7f" />
                </Viewbox>
                <Viewbox DockPanel.Dock="Right" Stretch="Uniform" Width="18"
                         Visibility="{Binding StorePasswordCommand.IsExecutable, Converter={StaticResource CollapsedWhenTrue}}">
                    <Path Data="{StaticResource Warning}" Fill="#db3232" />
                </Viewbox>
                <PasswordBox Name="PasswordBox" DockPanel.Dock="Left" Margin="0 0 5 0"
                             PasswordChanged="PasswordBox_OnPasswordChanged" TabIndex="0" />
            </DockPanel>
            <TextBlock Margin="0 5" Name="PasswordHintText" TextWrapping="Wrap" />
        </StackPanel>

        <Button adonisExtensions:RippleExtension.FadeInDuration="0" Grid.Row="1" Margin="10 10 5 10" Grid.Column="0" Name="OkButton" IsDefault="True" Command="{Binding StorePasswordCommand}" TabIndex="1">OK</Button>
        <Button adonisExtensions:RippleExtension.FadeInDuration="0" Grid.Row="1" Margin="10 10 5 10" Grid.Column="1" Name="SkipButton" Command="{Binding SkipCommand}" TabIndex="2">Skip</Button>
        <Button adonisExtensions:RippleExtension.FadeInDuration="0" Grid.Row="1" Margin="10 10 5 10" Grid.Column="1" Name="RemoveButton" Command="{Binding RemovePasswordCommand}" TabIndex="3">Remove</Button>
        <Button adonisExtensions:RippleExtension.FadeInDuration="0" Grid.Row="1" Margin="10 10 10 10" Grid.Column="2" IsCancel="True" Name="CancelButton" TabIndex="4">Cancel</Button>
    </Grid>
</adonisControls:AdonisWindow>